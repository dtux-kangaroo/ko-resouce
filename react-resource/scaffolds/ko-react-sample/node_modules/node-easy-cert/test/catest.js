const EasyCert = require('../src/index.js');

const options = {
  rootDirPath: '/Users/wangweijie/.anyproxy/certificates',
  defaultCertAttrs: [
    { name: 'countryName', value: 'CN' },
    { name: 'organizationName', value: 'AnyProxy' },
    { shortName: 'ST', value: 'SH' },
    { shortName: 'OU', value: 'AnyProxy SSL Proxy' }
  ]
};

const easyCert = new EasyCert(options);

easyCert.getCertificate('30.17.228.86', () => {

});
